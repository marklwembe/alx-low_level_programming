#include "main.h"

/**
 * _abs - Calculates absolute value of given number
 * @n: Integer number given
 * Return: absolute of number given
 */

int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-n);
}
