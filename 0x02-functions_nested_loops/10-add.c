#include "main.h"
/**
 * add _ Adds to numbers and retuns the sum
 * @a: First number
 * @b: Second number
 * Return: The sum of the two inputs
 */

int add(int a, int b)
{
	int result = a + b;
	return (result);
}